server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    root: INFO
    com.example.validadorfirmas: DEBUG
  file:
    name: logs/validador-firmas.log
